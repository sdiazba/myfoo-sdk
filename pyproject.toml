[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[project]
name = "myfoo-sdk"
version = "1.0.1"
description = "A minimal SDK for foo"
readme = "README.md"
authors = [{name = "Silviano Diaz"}]
dependencies = []